export default {
    evaluating: "condition:evaluating",
    succeded: "condition:success",
    failed: "condition:failed",
    timeout: "condition:timeout",
    aborted: "condition:aborted",
    error: "condition:error",
} as const;